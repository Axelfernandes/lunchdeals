version: 1.0
runtime: nodejs18
build:
  commands:
    - cd server && npm install
run:
  command: cd server && node index.js
  network:
    port: 3001
  env:
    - name: PORT
      value: "3001"
    - name: NODE_ENV
      value: "production"
